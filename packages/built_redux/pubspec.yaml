name: built_redux
description: Redux state management. Local fork for Dart 3 compatibility (runtime only, no codegen).
version: 7.5.11
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  built_collection: ^5.1.1
  built_value: ^8.9.2
