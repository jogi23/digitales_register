name: dr
description: A new Flutter project.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# Read more about versioning at semver.org.
version: 1.1.5+13

publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  badges: ^3.1.2
  biometric_storage: ^5.0.0
  built_collection: ^5.1.1
  built_value: ^8.12.6
  charts_common:
    path: packages/charts_common/
  charts_flutter:
    path: packages/charts_flutter/
  collapsible_sidebar:
    path: packages/collapsible_sidebar/
  collection: ^1.15.0
  cookie_jar: ^4.0.8
  deleteable_tile:
    path: packages/deleteable_tile/
  dio: ^5.7.0
  dio_cookie_manager: ^3.1.2
  dynamic_theme:
    path: packages/dynamic_theme/
  flutter:
    sdk: flutter
  flutter_colorpicker: ^1.1.0
  flutter_riverpod: ^2.6.1
  flutter_localizations:
    sdk: flutter
  flutter_secure_storage: ^10.1.0
  flutter_svg: ^2.3.0
  flutter_widget_from_html_core: ^0.17.2
  fuzzy: ^0.5.1
  hive: ^2.0.0
  http: ^1.2.0
  intl: ^0.20.2
  mutex: ^3.0.0
  open_file:
    git:
      url: https://github.com/miDeb/open_file.git
      ref: 5028bb18379df881d30b42c61b0e6e686daba60e
  package_info_plus: ^8.0.3
  path_provider: ^2.1.5
  quill_delta:
    path: packages/quill_delta/
  quill_delta_viewer:
    path: packages/quill_delta_viewer/
  responsive_scaffold:
    path: packages/responsive_scaffold/
  scroll_to_index: ^3.0.1
  scrollable_positioned_list: ^0.3.1
  sentry_flutter: ^9.20.0
  shared_preferences: ^2.5.5
  uni_links:
    git:
      url: https://github.com/miDeb/uni_links.git
      ref: 7d9c079b9b07e2eac5f1d25a08b40e7f0c5c6cf0
      path: uni_links/
  url_launcher: ^6.3.1
  flutter_markdown_plus: ^1.0.6
  image_picker: ^1.2.2

dependency_overrides:
  file: ^6.1.4
  fwfh_text_style: ^2.22.08+1
  intl: ^0.20.2

dev_dependencies:
  build_runner: ^2.15.0
  built_value_generator: ^8.12.5
  flutter_launcher_icons: ^0.14.3
  flutter_native_splash: ^2.4.7
  flutter_test:
    sdk: flutter
  golden_toolkit: ^0.15.0
  lint: ^2.8.0
  matcher: ^0.12.16
  mocktail: ^1.0.4
  msix: ^3.16.13
  quiver: ^3.2.2
  sentry_dart_plugin: ^3.3.0

sentry:
  upload_debug_symbols: true
  upload_sources: false
  project: digitales_register
  org: johannes-feichter
  # auth_token is read from the SENTRY_AUTH_TOKEN environment variable

flutter_launcher_icons:
  android: "launcher_icon"
  adaptive_icon_background: "#2b6c66"
  adaptive_icon_foreground: "assets/icon_foreground.png"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/index.png"

flutter_native_splash:
  color: "3a9488"
  color_dark: "1e5c56"

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec
# The following section is specific to Flutter.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true
  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
  #  - images/a_dot_ham.jpeg
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.io/assets-and-images/#resolution-aware.
  # For details regarding adding assets from package dependencies, see
  # https://flutter.io/assets-and-images/#from-packages
  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.io/custom-fonts/#from-packages

msix_config:
  display_name: DigiReg ST
  publisher_display_name: Johannes Feichter
  identity_name: 2097JohannesFeichter.DigiRegST # todo: check/fid
  publisher: CN=2508760A-808B-4829-867A-4DB53593750E
  msix_version: 1.0.1.0
  logo_path: assets/4.0x/transparent.png
  architecture: x64
  capabilities: "internetClient"
